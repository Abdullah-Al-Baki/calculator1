#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a=100, b=10, sum, sub, mul;
    sum=a+b;
    sub=a-b;
    mul=a*b;

    printf("Sum is = %d \n", sum);
    printf("Sub of first-second number is = %d \n", sub);
    printf("Multiplies is = %d \n", mul);
    return 0;
}
