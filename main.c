#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a=100, b=10, sum, sub;
    sum=a+b;
    sub=a-b;

    printf("Sum is = %d \n", sum);
    printf("Sub of first-second number is = %d \n", sub);
    return 0;
}
